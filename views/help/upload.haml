-content_for :title do
  Helping Ignore for #{@viewed_user.fullname}

-content_for :scripts do
  %script{src: "/js/application.js"}
  :javascript
    $(function() {
      Application.init(#{@viewed_user.id}, "PUT", "/help-others", "#{@viewed_user.identifier}");
    });

-content_for :jumbotron do
  = haml :'partials/help_others_jumbotron', layout: false

= haml :'partials/help_others_tabs', layout: false, locals: { active_tab: "upload" }

%div{class: "alert alert-info tiny-margins", role: "alert"}
  Download discovered specimen records, work offline, then upload new claims as a csv. A file containing at most
  %span{class:"font-weight-bold"} #{number_with_delimiter(Settings.helping_download_limit)} records is generated.
  If you have many more than this, you may work in batches.
  %br
  This routine does not update underlying specimen data nor does it update existing claims. If existing claims are present in the upload, these will be skipped.

%div{class:"row"}
  %div{class:"col-sm-12 col-lg-10 col-xl-8"}
    %img{src: "/images/candidates-instructions.png", alt: "Instructions for offline claims", class:"rounded img-fluid"}

%form{id:"form-wrapper", method: :post, action: "/help-others/#{@viewed_user.identifier}/upload-result", enctype: "multipart/form-data", "accept-charset": "UTF-8", class: "form mt-4"}
  - relaxed = params["relaxed"] == "1" ? "?relaxed=1" : nil
  %div{class:"form-group row"}
    %div{class:"col-sm-4 col-lg-2 col-form-label text-left text-sm-right"} Download discovered records
    %div{class:"col-sm-8 col-lg-10"}
      %a{href: "/help-others/#{@viewed_user.identifier}/candidates.csv#{relaxed}", class:"btn btn-primary"}
        %i{class:"fas fa-file-download"}
        Download
  %div{class:"form-group row"}
    %label{for:"candidate-file", class:"col-sm-4 col-lg-2 col-form-label text-left text-sm-right"} Upload csv with claims
    %div{class:"col-sm-8 col-lg-10"}
      %input{type:"file", class:"form-control-file", id:"candidate-file", name: "file"}
      %button{type:"submit", class:"btn btn-success mt-3"} Submit
