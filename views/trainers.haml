-content_for :title do
  Trainers

-content_for :scripts do
  %script{src: "/js/application.js"}
  :javascript
    $(function() {
      Application.init();
    });

-content_for :jumbotron do
  %div{class:"jumbotron jumbotron-fluid d-flex flex-wrap"}
    %div{class:"mr-auto p-2"}
      %h1{class:"h2"} Trainers
      %p.lead.text-muted
        #{number_with_delimiter(@pagy.count)} people who have attributed natural history specimens to their peers, mentors, and role models

- @results.in_groups_of(3).each do |group|
  %div{class: "card-deck"}
    - group.each do |u|
      - if u.nil?
        %div{class:"card border-0"}
      - else
        - cache_fragment(u.identifier + "-trainer") do
          = haml :'partials/user/card-trainer', layout: false, locals: { user: u }

- if @pagy.pages > 1
  %div{style:"overflow-y:scroll"}
    = pagy_bootstrap_nav(@pagy)