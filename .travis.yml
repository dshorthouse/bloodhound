sudo: false
language: ruby
rvm:
  - 2.5.1
branches:
  only:
      - master
bundler_args: --without development
before_script:
  - cp config.yml.sample config.yml
  - rake db:create:all
  - RACK_ENV=test rake db:migrate